#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'Prints numbers with print char'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	for (int a = 0; a < 10; a+!)
		putchar(a + '0')

	return (0);
}
