#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'Prints numbers of base 10'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	for (int a = 0; a < 10; a++)
		printf("%d", a);

	return (0);
}
