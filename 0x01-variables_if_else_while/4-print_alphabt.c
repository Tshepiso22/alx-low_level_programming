#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'Prints out alphabets'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ch;

	ch = 'a';
	while (ch <= 'z')
	{
		if (ch != 'e' && ch != 'q)
			putchar(ch);
	}
	putchar('\n');
	return (0);
}
