#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'Writes some words and return'
 *
 * Return: Always zero (Success)
 */
int main(void)
{
	char str[] = "Programming is like building a multilingual puzzle\n";

	puts(str);

	return (0);
}
